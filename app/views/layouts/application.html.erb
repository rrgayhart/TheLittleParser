<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Little Parser" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="top-bar">
      <ul class="title-area">
        <li class="name">
          <h1><%= link_to "The Little Parser", new_parse_path %></a></h1>
        </li>
      </ul>
    </nav>

    <div class="row">
      <div class="large-10 columns"><%= yield %></div>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>
